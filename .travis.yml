language: java
jdk: oraclejdk8
os: linux
sudo: false

env:
  - MAVEN_VERSION=3.3.9

script:
  - mvn --show-version --errors --batch-mode clean verify

after_success:
  - mvn clean cobertura:cobertura coveralls:report

cache:
    directories:
      - $HOME/.m2